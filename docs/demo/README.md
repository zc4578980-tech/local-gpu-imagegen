# Demo Evidence

## Real Codex Workflow Onboarding (Zero GPU)

The [workflow-onboarding evidence note](workflow-onboarding.md) summarizes one
retained installed Codex session that discovered current ComfyUI inventory,
inspected and registered one supported API workflow, fingerprinted the exact
checkpoint, and completed the separate trust-binding flow. It did not submit a
prompt, use the GPU, or produce the image below.

## Genuine Local-GPU Ordinary Route

The [original finalized PNG](real/final.png), [validated showcase manifest](real/showcase-manifest.json), and [evidence notes](real/README.md) retain one ordinary `sdxl-txt2img` SDXL/ComfyUI result. The package binds the original bytes, sanitized MCP result, route identity, full-resolution review, public rights, client record, exact hashes, and known limitations. It proves only this bounded result and makes no measured image-quality or performance claim.

The regional and two-stage composition routes are experimental, not part of the golden path, and are not fallback routes for this demo. Their retained negative evidence does not establish a visual-quality improvement.

## Simulated Protocol Demo

`preview-loop.gif` is a deterministic, model-free illustration of the control protocol. It shows a brief, exact route confirmation, a bounded first round, structured rejection with a preserve/change contract, and a candidate that still waits for the user's decision. It is separate from the genuine image and evidence package above.

It is not generated by an image model and is not evidence of model quality, GPU performance, client-host compatibility, or completed public acceptance. The machine-readable boundary and GIF digest are retained in `demo-manifest.json`.

Regenerate it with Python 3.11 or later and no third-party dependencies:

```shell
python scripts/build_demo.py
```
